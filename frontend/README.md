# MathChrono — frontend

Vue 3 + Vite app. See the root [README.md](../README.md) for full project documentation.

```bash
npm install   # install dependencies
npm run dev   # start dev server at http://localhost:5173
npm run build # production build → dist/
```
